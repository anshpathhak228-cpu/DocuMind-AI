 "use client";
import {useState} from "react"; import Link from "next/link";
export default function Assistant(){const[q,setQ]=useState("");const[m,setM]=useState([{r:"ai",t:"Hi! I'm your DocuMind AI assistant. The RAG backend will retrieve relevant document chunks before answering."}]);function send(){if(!q.trim())return;setM(x=>[...x,{r:"user",t:q},{r:"ai",t:"Question received. Connect the production AI/RAG API to generate the document-grounded answer."}]);setQ("")}return <main><div className="container"><nav className="nav"><Link className="brand" href="/dashboard">Docu<span>Mind</span> AI</Link><div className="links"><Link href="/dashboard">Dashboard</Link><Link href="/upload">Upload</Link></div></nav><section className="main"><h1>AI Assistant</h1><p className="muted">ChatGPT-style workspace.</p><div className="card chat"><div className="messages">{m.map((x,i)=><div className={`msg ${x.r}`} key={i}>{x.t}</div>)}</div><div className="composer"><input className="input" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Ask AI anything…"/><button className="btn primary" onClick={send}>Send</button></div></div></section></div></main>}
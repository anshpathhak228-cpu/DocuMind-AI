 "use client";
import {useState} from "react"; import Link from "next/link"; import {createClient} from "@/lib/supabase";
export default function Login(){const[email,setEmail]=useState("");const[sent,setSent]=useState(false);const[err,setErr]=useState("");async function go(e:React.FormEvent){e.preventDefault();setErr("");try{const sb=createClient();const{error}=await sb.auth.signInWithOtp({email,options:{shouldCreateUser:true}});if(error)throw error;setSent(true)}catch(x){setErr(x instanceof Error?x.message:"Unable to send OTP")}}return <main className="auth"><div className="authbox"><Link href="/" className="muted">← DocuMind AI</Link><h1>Sign in</h1><p className="muted">Secure email OTP authentication.</p>{sent?<div className="card"><h3>OTP sent</h3><p>Check your email to complete authentication. Configure Supabase Auth for the final verification flow.</p></div>:<form className="stack" onSubmit={go}><label>Email</label><input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/>{err&&<p style={{color:"#ff9c9c"}}>{err}</p>}<button className="btn primary">Send OTP</button></form>}</div></main>}